# Core dependencies
-r requirements.txt  # Ensures the main requirements are also installed

# Development and testing dependencies
flake8==6.1.0       # For code linting
pytest==7.4.2       # For running tests
pytest-asyncio==0.21.0  # For async testing support
black==23.9.1       # For code formatting
isort==5.12.0       # For import sorting
